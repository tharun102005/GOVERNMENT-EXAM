/**
 * antiGravity.js
 * Disabled anti-gravity engine to remove all card tilting, repulsion, and mouse animations.
 */

export function initAntiGravity() {
  return () => {};
}

export const mouse = { x: 0, y: 0 };
